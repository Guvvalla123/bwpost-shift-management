# Baked in by `npm run build` (Vite production mode). Committed so deploys have a default API target.
# Override in Vercel/CI with VITE_API_URL if needed.
VITE_API_URL=https://bwpost-shift-management-backend.onrender.com
